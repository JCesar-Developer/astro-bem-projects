---
import OhStudio from '@/layouts/oh.studio.astro';
import Tag from '@oh.studio/components/tag.astro';
import CardExperience from '@oh.studio/components/card-experience.astro';

import Experiences from '@oh.studio/data/experiences.data';
import Clients from '@oh.studio/data/clients.data';

const rootUrl = import.meta.env.OH_STUDIO;
---

<OhStudio title='profile'>
  <section class="hero">
    <h1 class="hero__title">Hey 👋🏼 I'm Oli </h1>
  </section>

  <section class="pictures">
    <img
      class="pictures__image" 
      src={ `${rootUrl}/oli_1.jpg` } 
      alt="Profile picture"
    >
    <img
      class="pictures__image" 
      src={ `${rootUrl}/oli_2.jpg` }
      alt="Profile picture"
    >
  </section>

  <section class="about">
    <Tag text="About" />
    <h2 class="about__description">A freelance designer based in the UK. I combine my experience in product and brand to solve problems, tell stories, and create compelling experiences.</h2>
  </section>

  <section class="experience">
    <Tag text="Experience" />
    <h2 class="experience__description">Where I've worked</h2>
    <div class="experience__grid">
      {
        Experiences.map(experience => 
          <CardExperience 
            period={ experience.period }
            enterprise={ experience.enterprise }
            position={ experience.position }
            contract={ experience.contract }
          />
        )
      }
    </div>
  </section>

  <section class="clients">
    <Tag text="Clients" />
    <h2 class="clients__description">Who I've worked with</h2>
    <div class="clients__grid">
      { Clients.map(client =>
        <img class="clients__client" src={ client.logo_url } alt={ client.description } />
      )}
    </div>
  </section>
  
  <section class="prefooter">
    <h2 class="prefooter__title">Let's work together.</h2>
    <h2 class="prefooter__title">
      <a class="prefooter__actionbtn" href={ `${ rootUrl }/contact` }>
        Get in touch.
      </a>
    </h2>
  </section>
</OhStudio>

<style>
  .hero {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 80vh;

    .hero__title {
      animation-name: show-up;
      animation-duration: 1s;
    }
  }

  .pictures {
    width: auto;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-gap: 1rem;

    .pictures__image {
      width: 100%;
      aspect-ratio: 1/.75;
      object-fit: cover;
      border-radius: 15px;

      animation-name: show-up;
      animation-duration: 1s;
      animation-delay: .3s;
      animation-fill-mode: both;
    }
  }

  .about {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    height: 90vh;

    .tag {
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 20% 60%;
    }

    .about__description {
      margin-top: 1rem;
      max-width: 600px;

      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 25% 60%;
    }
  }

  .experience {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;

    >.tag {
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 30% 60%;
    }

    .experience__description {
      margin-top: 1rem;
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 35% 60%;
    }

    .experience__grid {
      display: grid;
      width: 100%;
      grid-template-columns: repeat(3, 1fr);
      row-gap: 5rem;
      align-items: center;
      margin: 7rem 0;
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 10% 30%;
    }
  }

  .clients {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;

    >.tag {
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 30% 60%;
    }

    .clients__description {
      margin-top: 1rem;
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 35% 60%;
    }

    .clients__grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      width: 100%;
      margin: 7rem 0;
      animation-name: show-up;
      animation-fill-mode: backwards;
      animation-timeline: view();
      animation-range: 10% 30%;

      .clients__client {
        width: 100%;
        padding: 20px;
        filter: invert(1);
      }
    }
  }

  .prefooter {
    margin-top: 2rem;
    margin-bottom: 10rem;
    text-align: center;

    .prefooter__title {
      font-size: 2.5rem;
      font-weight: 400;

      animation-name: show-up;
      animation-fill-mode: both;
      animation-timeline: view();
      animation-range: 150px 250px;
    }

    .prefooter__actionbtn {
      color: var(--oh-primary);
      cursor: pointer;
      transition: color .1s ease-in-out;

      &:hover {
        color: var(--oh-secondary);
      }
    }
  }
</style>